---
/**
 * MeshBackground Component
 *
 * High-performance CSS mesh gradient background for the hero section.
 * Replaces heavy 3D scenes with a smooth, algorithmic gradient animation.
 *
 * PRD Requirements (Section 2.1):
 * - "Algorithmic Mesh Gradients" using complex CSS background-image
 * - Colors: Forest Green, Neon Growth Green, Deep Soil Dark
 * - No battery drain from 3D rendering
 * - Smooth CSS transitions only
 *
 * Visual Strategy:
 * - Multiple radial gradients layered and positioned
 * - Subtle animation using CSS keyframes
 * - Mobile-optimized (GPU-accelerated transforms only)
 */
---

<div class="mesh-background"></div>

<style>
  .mesh-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    z-index: 0;
    overflow: hidden;

    /* Algorithmic Mesh Gradient - Layered radial gradients */
    background-color: #051F15; /* Deep Soil Dark (base) */
    background-image:
      /* Neon Growth Green accents */
      radial-gradient(ellipse at 20% 30%, rgba(0, 255, 148, 0.12) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 20%, rgba(0, 204, 118, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse at 40% 70%, rgba(0, 255, 148, 0.1) 0%, transparent 45%),

      /* Forest Green layers */
      radial-gradient(ellipse at 60% 50%, rgba(26, 77, 46, 0.3) 0%, transparent 60%),
      radial-gradient(ellipse at 10% 80%, rgba(10, 47, 31, 0.4) 0%, transparent 55%),
      radial-gradient(ellipse at 90% 70%, rgba(26, 77, 46, 0.25) 0%, transparent 50%),

      /* Deep Soil Dark depth layers */
      radial-gradient(ellipse at 50% 40%, rgba(5, 31, 21, 0.6) 0%, transparent 70%),
      radial-gradient(ellipse at 30% 10%, rgba(15, 61, 40, 0.3) 0%, transparent 65%);

    background-size:
      120% 120%,
      140% 140%,
      100% 100%,
      110% 110%,
      130% 130%,
      90% 90%,
      150% 150%,
      100% 100%;

    background-position:
      0% 0%,
      100% 0%,
      50% 100%,
      0% 50%,
      100% 100%,
      50% 0%,
      0% 100%,
      50% 50%;

    /* Subtle animation (battery-efficient) */
    animation: meshShift 20s ease-in-out infinite alternate;
  }

  /* Subtle gradient shift animation */
  @keyframes meshShift {
    0% {
      background-position:
        0% 0%,
        100% 0%,
        50% 100%,
        0% 50%,
        100% 100%,
        50% 0%,
        0% 100%,
        50% 50%;
    }
    100% {
      background-position:
        10% 10%,
        90% 10%,
        60% 90%,
        10% 60%,
        90% 90%,
        60% 10%,
        10% 90%,
        60% 60%;
    }
  }

  /* Disable animation if user prefers reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .mesh-background {
      animation: none;
    }
  }

  /* Add subtle texture overlay for visual interest */
  .mesh-background::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
    background-size: 20px 20px;
    pointer-events: none;
    opacity: 0.5;
  }
</style>
