---
import GlassCard from '../../ui/glass/GlassCard.astro';
import HoverLift from '../../ui/effects/HoverLift.astro';
---

<section id="process" class="relative py-32">
  <div class="container mx-auto max-w-7xl px-6">
    <div class="section-header">
      <h2 class="text-4xl md:text-5xl font-bold font-mono mb-4">
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-neon-400 to-accent-blue">How It</span> Works
      </h2>
      <p class="subtitle font-mono text-text-muted">From application to launch in three simple steps</p>
    </div>

    <div class="process-grid relative">
      <!-- Connecting Line (Desktop) -->
      <div class="hidden md:block absolute top-1/2 left-0 w-full h-1 bg-gradient-to-r from-neon-400 via-accent-blue to-secondary opacity-20 -translate-y-1/2 z-0"></div>

      <HoverLift>
        <div class="relative pt-6 h-full">
          <div class="absolute top-0 left-1/2 -translate-x-1/2 w-12 h-12 rounded-full bg-forest-900 border-2 border-neon-400 flex items-center justify-center font-bold font-mono text-neon-400 z-20 shadow-[0_0_15px_rgba(0,255,148,0.3)]">1</div>
          <GlassCard variant="card" class="process-step p-8 pt-10 h-full relative z-10">
            <h3 class="text-2xl font-bold font-mono text-neon-400 mb-4 mt-2 text-center">Apply</h3>
            <p class="text-text-muted text-center leading-relaxed">Fill out our simple application form to join the waitlist. We'll get back to you within a few days to schedule an initial conversation.</p>
          </GlassCard>
        </div>
      </HoverLift>

      <HoverLift>
        <div class="relative pt-6 h-full">
          <div class="absolute top-0 left-1/2 -translate-x-1/2 w-12 h-12 rounded-full bg-forest-900 border-2 border-accent-blue flex items-center justify-center font-bold font-mono text-accent-blue z-20 shadow-[0_0_15px_rgba(74,144,164,0.3)]">2</div>
          <GlassCard variant="card" class="process-step p-8 pt-10 h-full relative z-10">
            <h3 class="text-2xl font-bold font-mono text-accent-blue mb-4 mt-2 text-center">Collaborate</h3>
            <p class="text-text-muted text-center leading-relaxed">We'll work with you to understand your business and your goals. We believe in a collaborative, iterative process.</p>
          </GlassCard>
        </div>
      </HoverLift>

      <HoverLift>
        <div class="relative pt-6 h-full">
          <div class="absolute top-0 left-1/2 -translate-x-1/2 w-12 h-12 rounded-full bg-forest-900 border-2 border-secondary flex items-center justify-center font-bold font-mono text-secondary z-20 shadow-[0_0_15px_rgba(232,153,92,0.3)]">3</div>
          <GlassCard variant="card" class="process-step p-8 pt-10 h-full relative z-10">
            <h3 class="text-2xl font-bold font-mono text-secondary mb-4 mt-2 text-center">Launch</h3>
            <p class="text-text-muted text-center leading-relaxed">We'll build your website within one week. After that, we'll provide 30 days of dedicated support to help you get running.</p>
          </GlassCard>
        </div>
      </HoverLift>
    </div>
  </div>
</section>

<style>
  .section-header {
    text-align: center;
    margin-bottom: 6rem;
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 3rem;
  }

  @media (max-width: 768px) {
    section {
      padding: 5rem 1.5rem;
    }
    
    .section-header {
      margin-bottom: 4rem;
    }

    .process-grid {
      grid-template-columns: 1fr;
      gap: 4rem;
    }
  }
</style>
