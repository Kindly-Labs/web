/**
 * @owly-labs/eslint-config
 *
 * Default export is the Next.js configuration.
 * For other configurations, import from:
 * - @owly-labs/eslint-config/base (TypeScript only)
 * - @owly-labs/eslint-config/react (React without Next.js)
 * - @owly-labs/eslint-config/next (Next.js apps)
 */

module.exports = require('./next');
